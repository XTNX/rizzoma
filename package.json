{
  "name": "rizzoma-app",
  "version": "0.2.0",
  "private": true,
  "description": "RIZZOMA — Telegram Mini App: дерево узлов, билеты, реферальная сеть. Статика без сборки; package.json нужен только автотестам.",
  "scripts": {
    "serve": "node tests/serve.mjs",
    "test": "node --test tests/economy.test.mjs",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  },
  "engines": {
    "node": ">=20"
  }
}
